/**
 * @swagger
 * definitions:
 *   User:
 *     type: object
 *     properties:
 *       userId:
 *         type: integer
 *       email:
 *         type: string
 *       password:
 *         type: string
 *         format: password
 *       firstName:
 *         type: string
 *       lastName:
 *         type: string
 *       gender:
 *         type: string
 *       jobRole:
 *         type: string
 *       department:
 *         type: string
 *       address:
 *         type: string
 *       required:
 *         - email
 *         - password
 */
